Algoritmo "07"

Var

numer, soma, num:inteiro

Inicio

para numer<-1 ate 10 faca
escreval
escreval("Digite o ",numer,"° número que deseja somar")
leia(num)
soma<-num+soma
fimpara

escreval
escreval("O resultado é:",soma)

fimalgoritmo
